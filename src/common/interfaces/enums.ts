export enum status {
  PENDING = 'PENDING',
  ORDERING = 'ORDERING',
  RECEIVED = 'RECEIVED',
  OPENED = 'OPENED',
  READY = 'READY'
}

export enum roles {
  ADMIN = 'ADMIN',
  BUYER = 'BUYER',
  SELLER = 'SELLER'
}
